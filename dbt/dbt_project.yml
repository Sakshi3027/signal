name: signal
version: '1.0.0'
config-version: 2

profile: signal

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  signal:
    staging:
      +materialized: view
    marts:
      +materialized: table
