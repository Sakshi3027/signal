[project]
name = "signal-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = [
  "pytest>=8.0",
  "ruff>=0.4",
  "python-dotenv>=1.0",
  "numpy>=2.4.6",
  "pandas>=3.0.3",
  "dbt-duckdb>=1.8",
]
